{
  "name": "share-log",
  "version": "1.0.0",
  "description": "LAN-P2P Local-First Project Tracker",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:exe": "npm run build && npx -y pkg . --out-path bin",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx src/ui/server.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.3.3",
    "tsx": "^4.7.1",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
